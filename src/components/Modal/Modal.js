import { ModalBox, ModalOverlay } from './Modal.styled';

export const Modal = () => {
  return (
    <ModalOverlay>
      <ModalBox>
        <img src="" alt="" />
      </ModalBox>
    </ModalOverlay>
  );
};
